import SwiftUI
import SwiftData

struct ContentView: View {
    var body: some View {
        VStack {
            Text("BmtcMate")
            Spacer()
        }
    }
}

#Preview {
    ContentView()
}
